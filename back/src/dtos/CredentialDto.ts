//* Una interfaz para las credenciales especificamente usada en los parametros, por eso no tiene ID.

interface CredentialDto {
  username: string;
  password: string;
}

export default CredentialDto;
